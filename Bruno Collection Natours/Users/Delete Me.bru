meta {
  name: Delete Me
  type: http
  seq: 3
}

delete {
  url: {{URL}}api/v1/users/deleteMe
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

settings {
  encodeUrl: true
  timeout: 0
}
